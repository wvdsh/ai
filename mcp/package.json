{
  "name": "@wvdsh/mcp",
  "version": "0.1.0",
  "private": true,
  "description": "Read-only Wavedash MCP server for public docs, SDK guidance, and publishing checklists.",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "worker:dev": "wrangler dev",
    "deploy:dry-run": "wrangler deploy --dry-run",
    "deploy": "wrangler deploy"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0",
    "zod": "^4.2.1"
  },
  "devDependencies": {
    "wrangler": "^4.54.0"
  }
}
